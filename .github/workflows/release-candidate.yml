name: Dagger release candidate
on:
  workflow_dispatch:
    inputs:
      tag:
        description: Existing vX.Y.Z tag identifying exact current main
        required: true
        type: string
permissions:
  contents: read
concurrency:
  group: assay-release-candidate
  cancel-in-progress: false
jobs:
  candidate:
    name: Dagger release candidate
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      actions: read # Upload only the exact candidate directory.
      checks: read # Dagger verifies one exact-SHA hosted gate.
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      # The dispatched tag is attacker-shapeable text. It never appears as a
      # `${{ }}` expression: it reaches the step only as the TAG environment
      # variable. dagger-for-github pastes `args` into bash, so every value there
      # is a double-quoted variable that bash expands as one inert word. Dagger
      # then rejects any tag that is not the metadata-derived tag on exact main.
      - uses: dagger/dagger-for-github@496f1b3d8b0d823834c13e67cf8a8e08ca3b9602 # v8.4.0
        env:
          GITHUB_TOKEN: ${{ github.token }}
          TAG: ${{ inputs.tag }}
        with:
          version: "0.21.8"
          verb: call
          args: release-candidate --tag="$TAG" --commit-sha="$GITHUB_SHA" --github-token=env:GITHUB_TOKEN export --path=candidate
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: assay-${{ github.sha }}
          path: candidate/
          if-no-files-found: error
          retention-days: 1
